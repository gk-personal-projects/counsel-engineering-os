# Core tier — intentionally no subagent files
<!-- Provenance: ORIGINAL (Apache-2.0). -->
In Core, the CTO IS the main session (constitution + always-on rules); Core work runs at
L0–L1 without delegation. Subagent seats begin at Builder. This file exists so the empty
directory is a statement, not an accident.
