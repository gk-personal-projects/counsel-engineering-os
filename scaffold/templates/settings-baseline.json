{
  "comment_provenance": "Counsel Engineering OS proposed permission baseline. ORIGINAL (Apache-2.0). NEVER applied without explicit user approval (D-T2-026 R5-S2: the plugin must not silently modify permissions). Conservative by design: deny-only protections plus one ask; grants nothing. Deny set upstreamed from the first governed install's security re-review (2026-08-12): destructive shell/git ops, fetch-pipe execution, git --output file-write vectors, depth-anchored secret reads.",
  "permissions": {
    "deny": [
      "Bash(rm -rf *)",
      "Bash(rm -r *)",
      "Bash(git reset --hard *)",
      "Bash(git clean -f *)",
      "Bash(git checkout -- .)",
      "Bash(git push --force *)",
      "Bash(git push --force-with-lease *)",
      "Bash(chmod 777 *)",
      "Bash(curl * | bash *)",
      "Bash(curl * | sh *)",
      "Bash(wget * | bash *)",
      "Bash(wget * | sh *)",
      "Bash(git diff --output*)",
      "Bash(git log --output*)",
      "Bash(git show --output*)",

      "Read(./.env)",
      "Read(./.env.*)",
      "Read(./**/.env)",
      "Read(./**/.env.*)",
      "Read(./.secrets/**)",
      "Read(./**/.secrets/**)",
      "Read(./**/*.pem)",
      "Read(./**/id_rsa*)"
    ],
    "ask": [
      "Bash(git push*)"
    ]
  }
}
