@AGENTS.md
<!-- Claude Code shim (Provenance: ORIGINAL, Apache-2.0). The constitution lives in
     AGENTS.md (harness-neutral); the import above loads it. Do not add content here —
     project law goes in AGENTS.md above the counsel:rules block. -->
